name: Ping URL Every 14 Minutes

on:
  schedule:
    # Run every 14 minutes (using cron syntax)
    - cron: '*/14 * * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  ping-url:
    runs-on: ubuntu-latest
    
    steps:
      - name: Ping URL
        run: curl -s -o /dev/null -w "%{http_code}" --max-time 60 https://motrix-ixyh.onrender.com/
